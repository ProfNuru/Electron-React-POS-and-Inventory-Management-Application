import styled from 'styled-components';

export const CustomerPerformanceChartComponent = styled.div`
    background-color: var(--light);
    box-shadow:var(--default-shadow);
    padding:2% 20px 5% 20px;
    margin: 20px 0px;
    width:100%;
    height:100%;

    & .chartHead{
        display:flex;
        flex-direction:column;
    }
`