import styled from 'styled-components';

export const MainSideNav = styled.nav`
    position: fixed;
    z-index:100;
    height:100vh;
    display:flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-evenly;
    background-color: var(--dark-shade);
`